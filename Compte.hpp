class Compte{

    std::string m_banque;

    float m_capital;

    int m_nbAn;

    float m_tauxAnnuel; 

    Compte(std::banque, float capital, int nbAn,float taux_annuel) : m_banque(banque), m_capital(capital), m_nbAn(nbAn), m_tauxAnnuel(taux_annuel);

}